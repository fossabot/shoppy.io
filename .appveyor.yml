os: Visual Studio 2015

environment:
  nodejs_version: "6.1"
  NODE_GYP_VER: 3.4.0
  NODE_GYP_DIR: '%USERPROFILE%\.node-gyp'

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install

test_script:
  - node --version
  - npm --version
  - npm test

build: off