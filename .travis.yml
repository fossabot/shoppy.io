language: node_js
node_js: stable

sudo: false
dist: trusty

cache:
  directories:
    - node_modules

matrix:
  include:
    - os: osx
      osx_image: xcode8.2

install: npm install
script: npm test

git:
  depth: 1
branches:
  only:
  - master
